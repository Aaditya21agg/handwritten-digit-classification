The project aims to identify handwritten digits using a simple neural network whose parameters like learning rate can be tweaked according to need. A confusion matrix is also plotted to show the performance of the model as well as the output also shows what were the actual digits and what the model actually guessed them to be.

